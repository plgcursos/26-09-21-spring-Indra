spring:
  application:
    name: s08-spring-data
  datasource:
    url: jdbc:h2:file:./datos/mdp;DB_CLOSE_DELAY=-1
    username: sa
  jpa:
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE